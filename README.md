littleprinter-stocks
====================

Print stock quotes on a Little Printer.

## Setup

Install dependencies from `requirements.txt` and run `stocks.py`. Set the
environment variable `DEBUG` to `1` if you need debug mode.
